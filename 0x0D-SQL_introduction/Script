sty all recosts all records in the table second_table of a database
SELECT score, name FROM second_table ORDER BY score DESC;rds in the table second_table of a database
SELECT score, name FROM second_table ORDER BY score DESC;

